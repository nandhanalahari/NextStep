# MongoDB Atlas – get from Atlas: Cluster → Connect → Connect your application
# Replace <password> with your DB user password; add database name in the path if you want a default DB
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Auth – generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-key-at-least-32-chars

# Gemini (for AI goal plans) – free key: https://aistudio.google.com/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your-gemini-api-key
